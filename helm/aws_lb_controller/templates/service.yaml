apiVersion: v1
kind: Service
metadata:
  name: {{ include "aws_lb_controller.fullname" . }}
  labels:
    {{- include "aws_lb_controller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aws_lb_controller.selectorLabels" . | nindent 4 }}
